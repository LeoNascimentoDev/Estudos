import React from "react";

// props faz referência aos atributos passados no index.js neste caso "name"
const Mycomponent = props => <h2>Testing atributes, the name of the atribute is: {props.name} </h2>

export default Mycomponent