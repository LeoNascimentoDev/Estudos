import React from "react";

const comp = props =>
    <React.Fragment>
        <h2>
            To use more then one html element we need to use dome div or the "React.Fragment" like a tag to envolve the code.
        </h2>
        <h3>Ok, It's working!</h3>
    </React.Fragment>

export default comp